<Window x:Class="ReadX.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReadX.Views"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="ReadX"
        Height="520"
        Width="760"
        MinHeight="480"
        MinWidth="680"
        Background="#111827"
        Foreground="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="ReadX" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="Ctrl+Shift+R" Foreground="#94A3B8" FontSize="13" />
            </StackPanel>

            <ui:Button x:Name="CaptureButton"
                       DockPanel.Dock="Right"
                       Width="150"
                       Height="42"
                       HorizontalAlignment="Right"
                       Appearance="Primary"
                       Content="Capture"
                       Icon="{ui:SymbolIcon Fluent24}"
                       Click="CaptureButton_Click" />
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="1.4*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1F2937" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Hotkey" Foreground="#94A3B8" FontSize="12" />
                    <TextBlock x:Name="HotkeyStatusText" Text="Ready" Margin="0,6,0,0" FontSize="16" FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#1F2937" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="OCR" Foreground="#94A3B8" FontSize="12" />
                    <TextBlock x:Name="OcrStatusText" Text="Ready" Margin="0,6,0,0" FontSize="16" FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#1F2937" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0">
                        <TextBlock Text="Words per minute" Foreground="#94A3B8" FontSize="12" />
                        <TextBlock x:Name="WpmValueText" DockPanel.Dock="Right" Text="300" FontSize="16" FontWeight="SemiBold" />
                    </DockPanel>
                    <Slider x:Name="WpmSlider"
                            Grid.Row="1"
                            Minimum="100"
                            Maximum="800"
                            TickFrequency="50"
                            Value="300"
                            IsSnapToTickEnabled="True"
                            Margin="0,12,0,0"
                            ValueChanged="WpmSlider_ValueChanged" />
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Last OCR" FontSize="15" FontWeight="SemiBold" />
                    <ui:Button x:Name="ReplayButton"
                               DockPanel.Dock="Right"
                               Width="132"
                               Height="34"
                               HorizontalAlignment="Right"
                               Content="Replay last"
                               Icon="{ui:SymbolIcon Fluent24}"
                               Click="ReplayButton_Click"
                               IsEnabled="False" />
                </DockPanel>

                <TextBox x:Name="LastOcrTextBox"
                         Grid.Row="1"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         BorderThickness="0"
                         Background="#111827"
                         Foreground="#CBD5E1"
                         Text="No capture yet." />
            </Grid>
        </Border>

        <TextBlock x:Name="StatusText"
                   Grid.Row="3"
                   Margin="0,14,0,0"
                   Foreground="#94A3B8"
                   Text="Ready." />
    </Grid>
</Window>
